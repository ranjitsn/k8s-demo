name: Hello World
on: 
  push:
    branches:
	- main
jobs:
  first-Hello-World-job:
   runs-on: ubuntu-latest
   steps:
	- name: checkout code
	  uses: actions/checkout@v3
	- name: print Message
	  run: echo "Hello World GitHub Actions NEW"
